[package]
name = "noise-tests"
version = "0.1.0"
authors = { workspace = true }
description = "Cross-backend test suite every Noise storage backend must pass"
repository = { workspace = true }
homepage = { workspace = true }
license = { workspace = true }
edition = { workspace = true }

[dependencies]
noise-storage = { workspace = true }
noise_search = { workspace = true }
include_dir = "0.7"
