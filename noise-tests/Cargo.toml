[package]
name = "noise-tests"
version = "0.1.0"
authors = ["Volker Mische <volker.mische@gmail.com>"]
license = "MIT OR Apache-2.0"
description = "Cross-backend test suite every Noise storage backend must pass"
edition = "2021"

[dependencies]
noise-storage = { path = "../noise-storage" }
noise_search = { path = "../noise-search" }
include_dir = "0.7"
